import React from 'react';

const Answer = () => {
    return (
        <div>
            <h3>How does react work</h3>
            <p><b>Answer:</b>React is a UI library made by facebook to build UI of a wevbsite.It is a component based framework and each component returns an element which is HTML element.</p>
        </div>
    );
};

export default Answer;